import React from 'react';

const Logout = (props) => (
    <div>
        <a href="/users/log-out" className="waves-effect waves-light btn-large transparent"><i className="material-icons left">exit_to_app</i></a>
    </div>
)

export default Logout;